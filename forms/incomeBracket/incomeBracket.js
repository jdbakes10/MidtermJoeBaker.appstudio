/*
//submit button
btnSubmit.onclick=function(){
  let income = inptIncome.value
  let taxBracket = `With your income of $${income}, you are in a tax bracket of ` 
    if (income < 30000) {
      lblTaxBracket.textContent = taxBracket + '8%'
  } else if (income >= 30000 && income < 99999) {
      lblTaxBracket.textContent = taxBracket + '15%'
  } else if (income >= 99999) {
      lblTaxBracket.textContent = taxBracket + '25%'
  }
}
//go again button 
btnGoAgain.onclick=function(){
  lblTaxBracket.value = "Please enter another income"
}
*/